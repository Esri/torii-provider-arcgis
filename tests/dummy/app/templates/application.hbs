<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

        {{link-to 'torii-provider-arcgis' 'index' class="navbar-brand"}}

    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li>{{link-to "Home" "index"}}</li>
        <li>{{link-to "Secure" "secure"}}</li>
        <li>{{link-to "GateKeeper" "gatekeeper"}}</li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isTokenAuth}}
          {{#if session.isAuthenticated}}
            <li><a href="#" {{action 'signout'}}>Sign Out</a></li>
          {{else}}
            <li>{{link-to "Sign In" "signin"}}</li>
          {{/if}}
        {{/if}}
        <li><a href="https://github.com/esri/torii-provider-arcgis">Github</a></li>
      </ul>
    </div><!--/.nav-collapse -->
  </div><!--/.container-fluid -->
</nav>

{{outlet}}
